export default function appendToEachArrayValue(array, appendString) {
  const appendedArray = [];
  for (const idx of appendedArray) {
    appendedArray.push(appendString + idx);
  }
  return appendedArray;
}
